on:
push:
branch: main

permissions:
content: write

jobs:
deploy:
runs on: ubantu-latest

steps:
- name: checkout repository
uses: actions/checkout@v3

- name: Setup Node.js
uses: actions/setup-node@v3
with:
node-version:18

- name: install dependencies
run: npm install

- name: Build react app
run: npm run build

 - name: Deploy to GitHub Pages
peaceiris/actions-gh-pages@v3
with:
github_token: ${{secret.DEPLOY}}

publish_dir. ./build

















name: Deploy React App to GitHub Pages

on:
  push:
    branches:
      - main
 
permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build React app
        run: npm run build

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.SIMBA }}
          publish_dir: ./dist
